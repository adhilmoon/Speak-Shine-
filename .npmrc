# Skip building optional native modules (canvas, sharp, etc.)
ignore-scripts=false
omit=optional

# Faster installs
prefer-dedupe=true
fund=false
audit=false
prefer-offline=true
cache-max=86400000
progress=false
